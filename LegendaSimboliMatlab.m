% .* Prodotto elemento per elemento
% ./ Divisione elemento per elemento
% * Moltiplicazione tra matrici (come in algebra)
% / Equivalente alla moltiplicazione per l'inversa (A * Inv(B))
% Si puo creare una struttura (structure) per raggruppare dati simili (tipo un oggeto
% python), es data.posizione, data.velocità, ...